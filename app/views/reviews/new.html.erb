<%= bootstrap_form_for(@reviews) do |f| %>
  <%= f.text_field :description%>
  <%= f.fields_for :review_group do |d| %>
	  <%= d.hidden_field :seller_id, value: @seller.id   %>
	  <%= d.hidden_field :user_id, value: current_user.id  %>
	<% end %>
  <%= f.submit "create review" %>
<% end %>